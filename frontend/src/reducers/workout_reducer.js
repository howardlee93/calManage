// workout_reducer.js


const workoutReducer = (state, action) => {
    Object.freeze(state);
    
    switch(action.type){
        

        default:
            return state;
    }

}

export default workoutReducer;
