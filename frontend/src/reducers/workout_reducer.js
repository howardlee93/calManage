// workout_reducer.js
//import actions

const initialState = [];




const workoutReducer = (state = initialState, action) => {
    Object.freeze(state);
    let newState;
    
    switch(action.type){
        

        default:
            return state;
    }

}

export default workoutReducer;
